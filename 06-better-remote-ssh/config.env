# Template image identity (sourced by build.sh and start.sh).
# Bump TAG before pushing a new version.
DOCKER_HANDLE=philippecharriere494
NAME=sbx-remote-ssh
TAG=0.0.0

# Docker-provided sandbox template to build the OpenSSH layer on top of.
# claude-code-docker already ships the claude agent + Docker-in-Docker.
BASE_IMAGE=docker/sandbox-templates:claude-code-docker
