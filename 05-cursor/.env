SANDBOX_NAME=cursor-ssh
SSH_HOST_ALIAS=sbx-cursor-ssh
SSH_PORT=2223
# Optional: absolute path to the `cursor` binary. Leave unset to auto-detect
# (PATH, then /Applications/Cursor.app/.../bin/cursor and other common spots).
# CURSOR_BIN=/Applications/Cursor.app/Contents/Resources/app/bin/cursor
